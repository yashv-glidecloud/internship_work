# Internship Daily Work Log

This repository contains my **day-wise internship work**, organized into separate folders for each day.  
Each folder includes the tasks, code, and experiments completed on that specific date.

---

## Folder Structure

### 📁 06-jan  
**FastAPI Task CRUD Application**

- Built a REST API using **FastAPI**
- Implemented **CRUD operations** (Create, Read, Update, Delete)
- Used **MongoDB** as the database
- Structured the project using:
  - `models.py` for data models
  - `routes.py` for API routes
  - `database.py` for database connection
- Tested APIs using **Swagger UI** and **Postman**

---

### 📁 07-jan  
**Vector Database Embedding with ChromaDB**

- Learned the fundamentals of **vector databases** and embeddings
- Used **Ollama** with `nomic-embed-text` to generate text embeddings
- Stored embeddings in **ChromaDB** with persistent storage
- Implemented:
  - Text embedding endpoint
  - File embedding endpoint
  - Semantic similarity search
- Built a **FastAPI service** to expose embedding and search APIs
- Understood **ANN-based vector search** concepts

---